#ifndef STATISTICS_H
#define STATISTICS_H

#include "Screen.h"

class Statistics {
public:  // Static 함수 구현
    static int totalRevenue(Screen* pScreen);
    static int totalSalesCount(Screen* pScreen);
};

#endif
